# Stage 1 门审查审计集（r15）测试计划

> **Author**: Super Z（QA-A/ARCH-A，Task 34-d）
> **Date**: 2026-09-11（r15，批次 E 收口）
> **依据**: sop §7.3/§7.3.1/§7.3.2/§7.1.1（七类矩阵）/§21.3（四条件）

## 1. 测试目标

Stage 1 阶段门审查的强制负向审计集（≥30 case + 七类覆盖 + 边界 ≥5）
——全部经**生产管线**（E1-β 切换后：自举 Reader + 自举 Expander 含宏）
执行，审计本身即 §21.3 条件 1/2 的持续验证载体。

## 2. 覆盖场景

| 桶 | 数量 | 内容 |
|---|------|------|
| A 单语句 | 12 | 基础类型系统负向（含宏解析错误/语法/未绑定/类型/空应用/向量 quote） |
| B 多语句 | 12 | 集成正确性（宏不匹配/覆盖糖名/模块导入/重复定义/提升头部校验） |
| C 复杂 | 10 | 嵌套递归/闭包/宏深度（自指 + 分叉扩散）/循环依赖（双模块源码 DFS）/prelude arity |
| D 恢复 | 6 | 错误后同进程续跑（read/expand/宏解析/循环依赖/深度/运行期/arity） |
| E 边界 | 6 | §7.3.2 本批次修复面（Span 代次守卫糖内宏/require·set 代次/prelude/模块内宏注册/卫生交换） |
| P 正向 | 4 | §21.3 锚定（fib 生产管线/prelude 管道/递归宏短路/缓存确定性 + 命中观测） |

§7.1.1 七类全覆盖（①语法 ②未绑定 ③空应用 ④参数个数 ⑤类型不匹配
⑥循环依赖 ⑦宏深度）；极性配比：负向 34（≥22）/ 恢复 6 / 正向 10。

## 3. 测试统计

- 50 case（examples/audit/stage1_gate_audit_r1.rs，`cargo run --example
  stage1_gate_audit_r1` 可重运行；配比在 main() 中机械校验，违规 →
  NEEDS REVISION 退出码 1）。

## 4. 审计发现项（r15 轮修复）

1. **模块注册簿错误无 Span 定位**（declare「重复声明」/visit「未声明
   的模块」/「循环依赖」此前 Span::dummy + 无渲染）→ 修复：挂 module
   形式 Span + render_diagnostic（B07/C01 case 驱动）；
2. 消息子串校准 8 处（审计预期与真实消息对齐——含 eval 路径深度上限
   先于未绑达的用例调整）。

## 5. 依赖

- kerf-driver 全公共 API（run/eval/compile/check）
- 双模块循环依赖经源码路径（无需子进程——DFS 灰标记结构化 Err）
