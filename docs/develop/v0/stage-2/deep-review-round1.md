# Stage 2 批次 I 深度审查报告（Round 1——I3 门审环）

> **审查日期**: 2026-09-11（r26）
> **审查者**: Super Z（ARCH-A 主导 + QA-A 验证 + REV-A 审查 + PM-A 协调 + ALG-C 语义确认——多角色会话）
> **基线版本**: kerf v0.1.0（git `f37782d` = r25 终态 + 本轮 I3 门审增量）
> **测试数**: 706:0:0（单元 207 + 集成 499——r25 终验口径）+ **stage2_gate_audit_r1 审计集 53 case EXIT 0（本轮新增）**

## 0. 定位声明（§1.2 路由产物——如实边界）

本轮为**批次 I 末门审深审**（plan.md §5a 42-g 合同：I3 门审查），非 Stage 2 大阶段末深审——Stage 2 余下面（效应 typecheck 收敛 / HM 生产切换 / FFI 实现 / 门控 net 行窗口 / §21.5 全阶段信号核对）属后续批次。本报告 D1-D8 按批次 I 面裁剪，D4 就绪度按「批次 J 输入」口径评估。Stage 2 大阶段末 §14.6 阶段间深验证（4 项强制审查 + 多轮深挖）在终批执行。

## 1. 执行摘要

- **一段话结论**：批次 I（I1 编译器 kerf 化收口 + I2 stdlib/GC/TD 批 + I 后段 Effect/能力 M2）全交付闭环，生产管线三段自举 + 门 B fixpoint 终验在位，706:0:0 零回归，门审计集 53 case APPROVED 零新发现——**批次 I 可收口（GO）**，进入 42-h 收尾交付。
- **阻塞项数量**: P0 = 0 / P1 = 0 / P2 = 0；P3 开放 4 项（TD-003/005/015/024——全部不影响批次 I 验收与批次 J 启动）。
- **建议行动**: **GO**（附条件：① P3 债维持登记册偿还计划；② pipeline-test-coverage 对账滞后一轮（r23/670 → r25/706）——47-z 收尾对账；③ 性能非尾形 +6.2% 带外 1pct 维持归因入册，Stage 2 末 GC 窗口复评）。

## 2. 八维度审查结论（§14.2——现状 + 风险 + 建议）

### D1. 架构健康度（§11 接口隔离）

- **现状**: 工作区 10 crate 单向依赖（span/syntax/core → reader/expander/compiler → vm/runtime → driver/backend），§11 合规零违规（r10 架构合规审计 4 测试持续全绿 + 本轮 §7.3 审计集 53 case 经生产管线全过）。自举三件套 install_state/reset_state 钩子（r23）为 fixpoint B₂ 语义引入受控全局状态面——审计集 E01/P03 探针实测状态隔离纪律（关缓存 + fresh 起步 + 探针尾复位）无泄漏。I1 迁移未引入跨阶段耦合：compiler.krf 镜像 compile.rs 十臂语义，桥（bootstrap_compiler.rs）保持两向转换纯函数形态。
- **风险**: 自举三件全局状态（thread_local）在并行测试下的隔离依赖 thread 天然分线程——当前 cargo test 并行全绿实证无互扰；未来跨线程共享需求出现时需重审（无此需求前不动——§2.2 最小面）。
- **建议**: 维持现状；批次 J 若引入效应 typecheck 收敛（Perform/Handle 不再 → Unknown），变更应限于 typecheck.rs 单点（预计 ≤3 文件——§14.5.3 设计缺陷处理口径内）。

### D2. 技术债清单

| ID | 描述 | 优先级 | 偿还计划 |
|----|------|--------|---------|
| TD-003 | 图 IR 复合节点 CSE 共享 | P3 | Stage 2 切换期重构（优化类——非正确性） |
| TD-005 | syntax-parse 级宏组合 | P3 | Stage 2 后续（宏系统增强批次） |
| TD-008 | 分代 GC / 堆压缩 | P3（DEFER r24——实测依据三面） | Stage 3+ 条件触发重评估 |
| TD-015 | IrGraph 无条件计算旁路丢弃 | P3 | Stage 2 切换期（与 TD-003 同窗口） |
| TD-024 | 本地码后端 PoC 边界（整数域 + B1 显式拒绝含效应） | P3 | Stage 2 批次 H-I（GC-后端协同）；native 效应化属 Stage 3 后端演进评估 |
| TD-001/006/019/020 | 编号断档（不可考） | — | 存档（r3 裁定——后续从 TD-015 起编） |

- **现状**: 开放 P3 共 4 项 + DEFER 1 项 + 断档 4 项；**P0/P1/P2 = 0**。批次 I 期间 resolved 8 项（TD-007/009/010/011/014/017/018/023 + TD-021/022/025 于 r15-r18）——偿还速率健康。
- **风险**: 无影响批次 J 的项；TD-024 native 效应显式拒绝（B1 类）与批次 J 效应收敛无耦合（VM 路径承载）。
- **建议**: 按登记册计划执行；无升级项。

### D3. 测试覆盖深度（正负比例——§14.5.1 完成标准 #8）

- **现状**: 套件 706:0:0（单元 207 + 集成 499——runner 单一总入口 mod 树）。**门审计集（本轮新增）53 case**：负向 34 + 恢复 6 + 边界 7 + 正向 6，§7.1.1 七类全覆盖（1=1 2=4 3=2 4=3 5=12 6=1 7=2），§7.3.2 上轮修复边界 7 case（r23/r24/r25 修复面逐项锚定）**零发现**。套件内负例族：bootstrap_compiler_tests 边界负例（define 位置/module/require 形态）+ effect_tests 负例 7（E0007/E0008/E0009 + 形态/类型面）+ qbe_backend_tests 负例 12 + multi_error_recovery_tests 16 + 双审计集（stage0 41 + stage1 51 + stage2 53）。正负比例（§9.4.3 1:3+）：门审计集负向 34（含恢复/边界负向语料）: 正向 6 ≈ 1:5.7 ✓（审计集口径）；套件全域正负比按 matrix §3 统计面维持 ≥1:3 口径（r16 深审对账以来无负向裁撤）。
- **风险**: 效应 typecheck 面（Perform/Handle → Unknown）目前为设计性放宽（r25 D10 正交裁定）——typecheck 收敛属批次 J，收敛前 HM/静态面不诊断效应形态（如实：非缺陷，是演进矩阵排期）。
- **建议**: 批次 J 引入效应 typecheck 时补静态负例组（≥3）；无其他补测缺口。

### D4. 下一阶段就绪度（批次 J 输入——Stage 2 余下面）

| 批次 J 需求 | 当前状态 | 差距 | 解阻计划 |
|-------------|---------|------|---------|
| 效应语义承载（typecheck 收敛输入） | ✅ VM 三原型帧 + E0007-E0009 + 双路径 parity（r25） | 静态面 → Unknown（设计性放宽） | 批次 J typecheck 收敛 MUV |
| 能力门控泛化（net 行窗口） | ✅ IoFamily 归位 + 评估依据在档（r25 M2） | net 语音面未增行（裁定维持——Stage 2 末窗口） | Stage 2 末批次（效应成熟后） |
| HM 生产切换 | ⚠️ PoC 15 测试全绿（r18 H4） | 旗标期未切换（D8 阶段 2 轨道） | 批次 J 评估轮（切换 vs 继续 PoC） |
| FFI 实现输入 | ✅ 所有权模型冻结（r17）+ ForeignBox 追踪器协议（r24 TD-010） | 实现未启动 | Stage 2 后续批次 |
| GC 压力承载 | ✅ 六来源根集 + GcCell 对症 -27.1%（r24/r25） | 分代 GC DEFER（实测依据三面） | Stage 3+ 条件触发 |
| §21.3 条件 3（非 VM 后端） | ✅ QBE fib PoC 端到端（r17 + 本轮 P04 复验 exit 144） | PoC 边界（TD-024） | 批次 H-I 协同窗口（已排） |

- **现状**: 批次 I 输出全部就位（I1/I2/I 后段三段交付 + 门审 APPROVED）。
- **风险**: 无阻塞项；HM 切换评估需防「PoC 长期化」——建议批次 J 早期决策点。
- **建议**: 批次 J 规划轮（plan §5b 细化）以效应 typecheck 收敛 + HM 切换评估为首批 MUV。

### D5. 设计合理性

- **现状**: 三大设计文档（effect-language-design v1.1 §9 执行注记九条 / capability-model-design §7 M2 / i1-incision-migration-design v1.3）与实现闭环（r25 R4 三处回写 + r23 跨链符号裁定入档）。核心结构：① 效应三原型帧编排（T/H/B）+ continuation 四要素——无过度设计（正交于 TCO 帧复用，D8 实测 10 万深穿透）；② 能力模型层/族层分离（r19）+ IoGrant 别名兼容——冻结路径零删改（既有 capability 测试零改动实证）；③ I1 切口 INC1-INC8 裁定全部兑现（S1-S3 段序 + parity 三门 + CompilerKind 切换点 P1-P5）。设计不足项：无（发现的设计灰区均经 §14.8 B4 回写闭环）。
- **风险**: 评估无——evidence：门 B fixpoint 字节一致（确定性纪律）+ 双路径 parity 全链（非「孤立正确」——§7.2 Q1-Q6 全过）。
- **建议**: 维持设计冻结面；批次 J 新设计按 §13.1 对齐流程。

### D6. 性能与可扩展性

- **现状**: 性能基线在档（performance-baseline §4.2 r25 交错对拍——同会话成对差消除机器漂移）：fib +3.9% 带内 ✓ / gc_stress +4.5% 带内 ✓ / **非尾形 +6.2% 带外 1pct**——对照实验（退回两模式判据）不降反升 → 归因三源叠加（布局漂移 + 自举 krf 体量 + GcCell 三模式），非单点回归；如实入册不做推测性微优化（§2.3-11）。超线性残留（TD-023 resolved 时的 2×→3.4×）已归因帧栈结构性成本。本轮门审复核：**维持 r25 裁定**（非热点恶化 + 归因清晰 + Stage 2 末 GC 窗口有 TD-008 条件触发评估位）。
- **风险**: 自举 krf 体量增长（compiler.krf ~800 行）对编译时间的影响在带内（13.87s clean 构建 vs r24 ~11.5s——含 target 差异；包内自举 14.17s/13.81s 两轮实测）。
- **建议**: Stage 2 末 GC 分代窗口（TD-008 重评估）联动复评非尾形基准；无当前行动项。

### D7. 文档与知识传承

- **现状**: 六面对账文档（plan.md Status 至 r25 / matrix.md r25 表体 / pipeline-test-coverage r23 版 / TD 登记册 r24 口径 / RELEASE_NOTES r25 / worklog 双层 + rec 树 09_r25）。设计文档三篇与实现闭环（D5）。新 Agent 可凭 docs/ 树 + sop.md §1.2 路由进入执行态（本会话实证——恢复点从过期摘要纠偏到磁盘实况即靠文档对账）。隐性知识风险低（每轮 worklog 载决策依据条款号）。
- **风险**: pipeline-test-coverage 对账滞后一轮（r23/670 → 实际 r25/706，+36 未对账——r24 +14 + r25 +22）。
- **建议**: 47-z 收尾对账六面时补齐（已列 W2）。

### D8. 测试路径覆盖与流水线印证

- **现状**: pipeline-test-coverage.md 三层覆盖（Tier 1 单元 / Tier 2 集成 / Tier 3 端到端）+ parity 印证节 + 性能基线同步节（r23 口径 670）。流印证：双路径（生产自举链 vs 种子链）bytecode_equal 全结构 + 行为面值一致——read → expand → compile 三段每段有 parity 门（A 基础/扩展/fixpoint B）。门审计集（本轮）补生产路径七类错误 + 恢复 + 边界 + §21.3 四条件锚定（自举链上 END-TO-END）。
- **风险**: 分支覆盖缺口与 D3 同源（效应 typecheck 收敛前的 Unknown 放宽面）。
- **建议**: 47-z 对账更新表体至 706 口径 + 批次 J 收敛时同步补路径行。

## 3. 委员会投票（§14.3 流程第 6 步——§6.3 加权）

| 角色 | 投票 | 理由 |
|------|------|------|
| ARCH-A（2 票） | **GO** | 架构零违规（§11 持续全绿）；三设计文档实现闭环；自举状态面受控（fixpoint 隔离纪律实测） |
| DEV-A（1.5 票） | **GO** | I1/I2/I 后段三段迁移零回归交付；代码整理 C1-C6 全过（本轮补 2 处 catch-all 注释 + 2 处 clippy）；706:0:0 |
| QA-A（1 票） | **GO** | 审计集 53 case EXIT 0（七类全覆盖 + 边界 7 + 四条件锚定）；正负比 1:5.7（审计集口径）✓；恢复面 6/6 |
| ALG-C（1 票，Type System Theorist） | **GO** | 效应归约 R10/R11 落地与 06 体系一致；continuation 线性唯一性（E0008）语义正确；HM PoC 域内不误报（15 测试） |
| PM-A（1 票，协调） | **GO** | 批次 I 合同六字段逐项兑现（plan §5a 42-x 行）；D4 批次 J 就绪无阻塞；收尾 42-h 合同就位 |

**加权总票**: 5.5 / 5.5 = **100% ≥ 95%**（§6.3 准入线）——**外循环通过**。

## 4. 行动计划（42-h 收尾——GO 附随条件清单）

1. **W1 文档对账**（47-z）: pipeline-test-coverage r23→r25 增量对账（+36：r24 +14 + r25 +22）+ matrix/plan/RELEASE_NOTES/TD 登记册五面 r26 口径同步。
2. **W2 打包与包内自举**（47-z）: r26 tar.gz（§19.3 commit-then-package 正序）+ 包内 706 复跑 + CLI 冒烟。
3. **W3 web 同步**（47-z）: kerf-data r26 节点 + footer + download README + E2E。
4. **W4 P3 债维持**（批次 J+）: 按登记册计划（D2）。
5. **W5 性能归因维持**（Stage 2 末）: 非尾形 +6.2% 归因入册 + TD-008 窗口联动复评（D6）。

## 5. 结论（Round 1——批次 I）

**APPROVED / GO**：批次 I 全交付验证闭环（706:0:0 + 审计集 53 EXIT 0 + 四条件锚定 + 五角色全票）；进入 42-h 收尾交付。Stage 2 大阶段末深审（§14.6 四项强制 + 多轮深挖）留待 Stage 2 终批——本轮为批次级深审（§0 定位）。

## 6. 设计偏差清单（§14.8——B1-B4 对照 lang-design + 批次 I 设计三篇）

| 设计文档章节 | 偏差类型 | 偏差描述 | 最优判断 | 重构判断 | 回写动作 |
|-------------|---------|---------|---------|---------|---------|
| effect-language-design §2/§3（v1.1 + §9 注记） | 无 | M1-M5 落地与 D1-D12 裁定逐条一致（九条执行注记已回写闭环） | N/A | N/A | 无（已闭环） |
| capability-model-design §7（M2 别名兼容） | 无 | IoFamily 形状标记落地与冻结路径零删改一致；§10 回写义务完成 | N/A | N/A | 无（已闭环） |
| i1-incision-migration-design v1.3 §5-§7 | 无 | 门 A/B/C 三门全过（r21/r22/r23 终验 + 本轮 P03 复验）；INC1-8 全兑现 | N/A | N/A | 无（已闭环） |
| 01/lang-design §7.3（原语集 9→11） | 无 | r25 W1 回写完成 | N/A | N/A | 无 |
| 04-bytecode-vm 指令族（41→43） | 无 | r25 W5 回写 + opcode_count 冻结测试同步（r21 R4 修正 40→41 先行） | N/A | N/A | 无 |
| 18-diagnostics §6 码位表 | 无 | E0007-E0009 r25 W3 回写 | N/A | N/A | 无 |
| hm-inference-design（D8 阶段 2 轨道） | **B1** | 设计含生产切换轨道；实现停在 PoC（15 测试）——旗标期 | 设计合理（PoC→切换演进轨） | N/A（非重构——排期） | 纳入批次 J 评估轮（W 计划） |
| ffi-ownership-model（r17 冻结） | **B1** | 模型冻结已实现文档面；运行时 FFI 属 Stage 2 后续批次 | 设计合理（模型先行） | N/A | 后续批次计划（§21.3 条件 4 完整版） |
| 13-capability-matrix（net 语音面） | **B1** | 矩阵含 net 族规划；门控表 net 行未增（r25 评估裁定维持——依赖①效应就位已满足，窗口 Stage 2 末） | 实现裁定更优（零破坏纪律） | N/A | Stage 2 末批次增行（§7 评估注记在档） |
| 12-roadmap §2.5 演进矩阵 | 无 | r23 行 299 回写（eval 退役）闭环 | N/A | N/A | 无 |

**B2/B3/B4**: 零新发现（r25 R4 三处灰区回写 + r23 跨链符号裁定入档后无残留）——依据 §14.8.3 规则 2，「无偏差」条目附对照记录（上表前三行 + 六个「无」行）。

## 7. 本轮遵循原则

§7.3.1/§7.3.2（配比与边界——审计集 53 case 机械校验）；§7.3（门审协议——独立审计 + APPROVED/NEEDS REVISION 判定）；§14.5（D1-D8 三段式）；§14.8（B1-B4 偏差清单 + 回写闭环验证）；§14.9（C1-C6 整理——发现 2 处注释缺失 + 2 处 clippy 违规当场修复）；§14.6.1.1（catch-all 注释合规——vm.rs 2 处已有理由、hm.rs 2 处本轮补齐）；§6.3（加权投票 ≥95%）；§2.3-11（实测禁臆测——审计集全 case 经生产管线真实运行 + GATE 1 EXIT 0）；GATE 1（706:0:0 复验 + fmt 0 diff + clippy --all-targets --workspace 超集口径 0 警告）；R4（代码为准——C3/C1 修复）；§21.2（QBE 不入自举链——P04 探针经宿主编排口径）。
