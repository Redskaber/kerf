# r44 E5 S3 M-R 名面臂 + 内部关键字全栈一致性深审（Task 65-a/65-b/65-z/65-web）

> 一行：深审 D34-D40（四套名面八处不一致 + 一致性总则「一个语义一个名」+ M-R 六裁定两 NO-GO + 自举桥 tag 域重构裁定）+ 五面同词根实施（变体五件 + Keyword 枚举对齐 + kind_name 12 名表 + 桥 tag 六改 + 机器断言 ×2）——834:0:0 净 +2 + 五审计集 233。

## 交付零（深审轮——内部关键字全栈一致性）
- 四查询实证 tool-results/r44-search/（q1 Python `ast.If`/`ast.Lambda` 节点名与关键字同词根大生产实践 / q2 自举双实现同步 HN / q3 Racket `#%plain-lambda` 双轨活体实证——kerf 原则 29/30 已否决的 `#%` 家族病 / q4 PEP8-C# 统一命名工程共识）
- **四套名面现状矩阵**：表面关键字 / 自举桥 quote 符号与 tag / Rust ADT 变体名 / kind_name——**四语义 8 处不一致 + Keyword 枚举内自身不一致 3 处**（assign 域最严重：assign↔'set[第三形]↔SetBang↔"set!" 四套四形）；r43「三域零变更」裁定为**把债务当资产引用的过度引申**（域隔离是防误伤流程 ≠ 名面漂移合法——R4 文档-代码冲突修正）
- **D34-D40**：一致性总则（一个语义一个名——五面同词根 + 原则 31 精确化：形态分离 ≠ 名面漂移）/ M-R 六裁定（五件 GO + **If→Branch/Literal→Const 两件 NO-GO**——一致性判据否决 01 §8.3 原候选：判据先于先例操作实例）/ Keyword 枚举对齐（D36）/ kind_name 12 名表（D37）/ **桥 tag 域六改**（'lambda→'fn/'set→'assign/'begin→'do/'app→'apply/'lit→'literal/'var 维持——D38 自举桥重构 + parity 影响分析：tag 不入 bytecode 门 A 恒成立 + fixpoint 重建合法）/ S3 分臂（D39——r44 名面臂 + r45+ 结构臂）/ 六域候选空间完备性收敛证明（D40）
- 文档：**22 §13 新增**（D34-D40 全表 + 13.4 实施实录 + §13.3 正交性对账 25 名五要素表）+ 22 v1.7 / **01 v6.3**（§2 OCaml 规范块同词根化[规范↔代码互锚] + §8.3 修正版 + §8.5 五面锚）/ **17 v6.6**（原则 31 精确化——形态分离 ≠ 名面漂移）/ e5-plan v1.2（S3 分臂记录）/ 20 §7 注记 / matrix v0.1.0-r44 / RELEASE v0.11.0-r44 / v0.5-roadmap v0.8.0-r44

## 交付一（M-R 名面臂实施）
- **变体五件重命名**：expr.rs 枚举定义（Lambda→Fn/SetBang→Assign/Begin→Do/App→Apply/VarRef→Var）+ OCaml 规范块 + 变体文档注释；15 crate 文件 + 7 测试文件 ~244 处前缀引用（sed 词边界批量 + 枚举定义手工）+ 注释面全清扫（crates/tests/examples 旧名 grep 零残留）
- **Keyword 枚举对齐**（D36）：symbol.rs 变体 Fn/Assign/Do + **`Keyword::ALL` 25 名单源**（预内部化 for 循环与 roundtrip 断言共享——单源化改进）+ keyword_all_roundtrip_and_distinct 新测试
- **kind_name 12 名表**（D37）：fn/apply/if/var/literal/assign/define/do/module/require/perform/handle——architecture_audit 12 名表同步 + **s3mr_kind_name_keyword_face_penetration 新测试**（9 原语级 kind_name ∈ Keyword 注册面 + 3 内部名 ∉ 关键字面——五面穿透机器断言）
- **自举桥 tag 域重构（D38）**：bootstrap_compiler.rs（core_to_node tag 表五改：literal/apply/fn/assign/do）+ bootstrap_expander.rs（core_from_value 匹配臂五改 + 错误消息同步）+ expander.krf（'lit→'literal ×9 + 'lambda→'fn + 'set→'assign + 'begin→'do ×2 + 'app→'apply ×2 + 头部契约注释）+ compiler.krf（双 pass (eq tag …) 十处 + 头部契约注释）——四文件双侧对称迁移，一次编译通过零误伤（r43 三域教训的流程防御生效：分域 grep → 编译 → 测试三道闸）
- **r43 遗留顺手清偿**：namespace_tests unused import（kerf_vm::Value——r43 基线已存在的 clippy 债，--all-targets 口径下检出）删除 → clippy -D warnings 0
- **§3.2 六命令 clean 起步全绿**：clean 2622 files/826.1MiB → build --release **14.26s** 零告警 → check 0 → fmt 0 diff → clippy --all-targets -- -D warnings 0 → **test 834:0:0**（615 集成[614+1] + 219 单元[218+1]）+ 五审计集 EXIT 0 ×5（233）+ CLI 四路径（fib 75025/144 + macros 42 + check ok + E0021 端到端 REAL_EXIT 1 携「现代形式「fn」」）
- **五面 grep 终局对账**：Rust 代码面旧名 0 / 桥语料面旧 tag 0 / kind_name 12 名全现代——E0021 旧名指引表（driver.rs REMOVED_KEYWORD_NAMES）+ s2 负例组源码为合法历史引用（域分明）

## 交付二（打包 + web）
- r44 tar.gz：**kerf-stage3-v0.11.0-r44-e5s3mr-nameface-834tests.tar.gz**（git commit 正序 + 包内自举 834 复跑 + 审计 ×5 + qbe 在包）
- web 面同步（五面同词根叙述 + r44 版本口径 + footer 包清单 + download README r44 节）

## 里程碑
- **E5 S3 M-R 名面臂闭环**——「1970 表面」清算三腿闭合（S1 库面 r42 + S2 表面 r43 + S3 内部名面 r44：五面同词根终态——用户指令「暴露给用户的关键字应该和编译器内部的一致性」全栈兑现）；下一步 **E5 S3 结构臂 r45+**（Define 脱糖/Do→Let 链/Module 迁移/Var de Bruijn 索引化——字节码 parity 最严口径，分臂多轮[每臂独立 commit + parity 断言先行]）→ S4 收口（非正交点 3→≤1 + 门审 ≥30 case + E5 出口三条件对账）
