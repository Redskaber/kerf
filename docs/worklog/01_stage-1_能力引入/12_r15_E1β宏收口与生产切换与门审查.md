# 12_r15 E1-β 宏收口 + 生产切换 + TD-021 + Stage 1 门审查 APPROVED

**覆盖**：r15 · Task 34-a~34-e · 2026-09-11 · 记忆类：e+s+p

## 概述

批次 E 收口：expander.krf 宏系统全模式面（syntax-rules + 卫生 α +
深度 500 + **Span 并集代次守卫**）→ **生产切换**（compile_front 展开
段经 bootstrap_expander——读+展开两段全自举）→ TD-021（kerf-prelude
模块 forms 级合并注入）→ Stage 1 门审查 APPROVED（50 case 审计 + 四
条件锚定）；553:0:0（单元 184 + 集成 369）；r15 包交付。

## 压实摘要

- **宏机制**（34-a）：变换器注册表 = 名字单表（'rules | 'builtin——
  HashMap 替换语义：用户宏覆盖糖名 + 惰性糖注册 + 卫生基名回退对两
  类同权）；match-datum/sequence（通配/字面量同名/省略号零-多段-
  复合/嵌套列表/向量 trial）；instantiate（模式变量原样/省略号拼接
  跳格/α 重命名 num->str 递归/保留集 + 关键字豁免）；深度 = 链长
  语义（expand-form 入口快照/出口回滚）
- **代次守卫镜像**（实测发现——parity 失败暴露 Span::merge 的
  expansion_id 不等守卫）：节点形状 v2 = (tag s e exp scopes ...)；
  retag 代次 +1；全部构造器代次来源逐位镜像；core 节点携带代次 →
  E3「展开相位标记」诊断特性经切换保持
- **生产切换**（34-b）：ExpanderKind（Bootstrap | Seed）参数化——
  生产 = bootstrap_expander；种子 = 自举引导 + parity oracle；守护
  = 独立线程活性探针 is_loaded() + 宏产物代次 ≥1（实测判别）
- **TD-021**（34-c）：preamble.krf（kerf-prelude 模块）+ Stx 层
  import 扫描 + read 后 expand 前 forms 前置合并（独立 file_id——
  P1/P3/P5 三否决全规避）+ 多模块按序 declare + 主模块 visit + 无
  module 程序 main 兜底
- **门审查**（34-d）：stage1_gate_audit_r1（A12/B12/C10/D6/E6/P4，
  七类全覆盖 + §7.3.2 边界 6 + §21.3 四条件锚定——P04 缓存命中
  观测）；发现项 2 修复（模块注册簿错误挂 module Span + render_
  diagnostic——P2 诊断质量）；APPROVED + §6.3 五角色全票 GO
- **实测修复**（全程 §2.3-11）：foldl/for-each 序章补定义（r14 死
  代码激活）+ set/require/define-syntax 代次漏挂 ×3 + preamble 头部
  () 形状 + 多模块 declare 破坏无模块程序（main 兜底）+ 消息校准 8

## 溯源

- flat 详录：docs/worklog.md Task 34-a~34-e 节
- RELEASE_NOTES v0.3.0-r15 节（五交付）
- 测试：tests/v0/stage1/plan/{bootstrap_expander_tests.rs 36,
  prelude_tests.rs 7} + examples/audit/stage1_gate_audit_r1.rs 50
- 测试计划：docs/tests/v0/stage1/plan/{bootstrap-expander.md E1-β 增补,
  prelude.md, gate-audit-r15.md}
