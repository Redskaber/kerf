# 10_r13 TD-004 作用域集解析收口（批次 E 启动）

**覆盖**：r13 · Task 32-a/32-b · 2026-09-11 · 记忆类：e+s+p

## 概述

Stage 1 批次 E 首个 MUV：TD-004（P2）清偿——标识符解析从名称基切换为
Racket 式作用域集解析 `(name, scopes ⊆)` + max-cardinality，编译器与
eval 双路径一致（T1）。500 基线零回归 + 9 锚点测试（509:0:0）。

## 压实摘要

- **设计**：scope-set 语义权威 + 空作用域集全局兜底（空集 ⊆ 任意
  引用集 ⇒ 全局/内置/`$hyg$` 基名回退自动降级为回退路径）+ α 重命名
  保留为第二道卫生保险；注入不变式 = 绑定形式 fresh scope 深注入
  （绑定器 + 全体体形式）⇒ 内层绑定作用域集严格包含外层 ⇒
  max-cardinality 与帧序在良构程序上同解
- **实施**（跨 5 crate）：kerf-syntax `Stx::add_scope_to_all`；
  kerf-expander fresh_scope 分配器 + expand_lambda 注入（先于体
  展开——自由标识符穿透保持）+ VarRef/SetBang 携带引用作用域集；
  kerf-core `Lambda.param_scopes` + `free_var_occurrences`（名称版 =
  投影，单一定义）；kerf-compiler `resolve_var(name, scopes)` 帧序 +
  帧内 max-cardinality + 捕获描述符携带绑定作用域集；kerf-vm
  `ClosureValue::Eval.param_scopes` + `Env::lookup/set` 同一子集口径
- **语义判别点**：名称匹配但作用域不匹配 ≠ 解析命中——按 Racket
  语义判未绑定（VM → 全局兜底后「未绑定的全局变量」；eval →
  「未绑定变量」）
- **测试**：tests/v0/stage1/plan/scope_set_tests.rs（9：双路径正例
  5 + 负例 4——作用域不匹配三锚 + 宏引入不捕获）+ runner.rs 注册 +
  scope-set.md 测试计划
- **交付**：§3.2 六命令全绿（clean 起步）；审计集 EXIT 0；r13
  tar.gz（根 worklog.md 排除 + rec 树入包 + 包内自举验证 509:0:0）；
  web 同步（ROADMAP 批次 E 行 + 509 基线 + footer 版本链）；文档
  六处同步（TD 登记/matrix/03-macro-system/plan.md/scope-set.md/
  RELEASE_NOTES）
- **流程注记**：用户任务 1（worklog 机制迁移）复核 = 31-e 已完成
  （树文法/路由/未压实区间零/by-topic 全在位），本轮零返工

## 溯源指针

详录 = flat worklog.md Task 32-a / 32-b 节（六字段 + 逐命令实测
记录）；RELEASE_NOTES v0.3.0-r13 节；tech-debt-register TD-004
（已解决 r13）。
