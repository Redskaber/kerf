# r20 批次 I 执行启动：I1 切口评估与迁移设计（切口裁定 + parity 三门 + 切换点）

> Task ID: 42-a ~ 42-z · 2026-09-11 · 溯源：flat worklog（kerf/docs/worklog.md 尾部两条目）· RELEASE_NOTES v0.4.0-r20

## 概要（1:8 压缩——详录见 flat）

- **42-a（I1 切口评估与迁移设计——批次 I 首 MUV）**：
  i1-incision-migration-design.md 十节——现状基线 **12 实锚
  B1-B12**（compile_module 单一入口 :229 / 十臂全景 / 作用域解析
  机 match_bindings / **bytecode_equal :145 = §21.3 条件 2 现成
  机器判据**（全结构 PartialEq 含 debug_spans）/ 三件套先例×2 /
  **analyzing 不在生产编译路径**（front_from_core:353 主链无
  check——自举命题不依赖 typecheck）/ eval 参考路径独立 /
  **SCOPE-NEXT 入口复位 :1471**（逐调用确定性纪律实证——fixpoint
  先决）/ TCO 尾位穿线 / $hyg$N 回退 / 缓存键无 CompilerKind 维度 /
  全结构比较无灰区）+ 本体盘点表 S0-S6（compile.rs 798 迁移主体 /
  字节码域 534 留 Rust / typecheck+hm 1709 绑 42-f / eval.rs 391
  排退役）。
- **切口裁定 INC1-INC8**：INC1 切口 = **compile_module 单点**（三件
  套第三实例——compiler.krf + bootstrap_compiler.rs 桥 + 种子
  oracle；输入节点格式**复用 expander.krf 输出契约**——零新设计）；
  INC2 字节码域留 Rust（VM 宿主契约——15 §5.3 轴位）；INC3 组合根
  留 Rust（编排非编译逻辑）；INC4 值树契约（'prog/'proto/'op/'const
  节点 + 符号 str 携带桥回 intern）；INC5 判据 = bytecode_equal；
  **INC6 analyzing 段不迁**（I1 范围内——代码实锚：生产 run/compile
  管线不调用 check；迁移评估绑 42-f/HM 同轮——12 §2.5 行 309）；
  **INC7 eval 退役排 42-d**（12 §2.5 行 299「被编译器替换（I1
  范围）」——终态 VM 单路径 + T1 收口）；**INC8 ~80% 口径精确化**
  （读+展开+编译三段 100% kerf = §21.3 条件 1 机器口径；Rust ~20%
  = VM/运行时/桥/组合根/后端 FFI/测试基建）。
- **段序 S1-S3（DAG 无环）**：S1 = 42-b 基础八臂（Literal/VarRef/
  If/Begin/SetBang/Define/App/Lambda——含作用域机 + 闭包捕获 +
  回填，B3 最难段风险前置）→ S2 = 42-c module/require 面 + 糖全
  管线 parity（端到端口径：含糖源 → 双路径 bytecode_equal）→
  S3 = 42-d 生产切换 + 自举终局（fixpoint + eval 退役）。
- **parity 三门 A/B/C**：门 A 段 parity（bytecode_equal——基础组
  ≥8 + 扩展组 ≥12；语料特化四组：深尾递归/捕获链/点对递归/遮蔽）；
  门 B 自举一致性（§21.3 条件 2——自举链 B₁/B₂ **隔离运行**四程序
  逐一 bytecode_equal + 加强判据 B₀/B₁ 种子-自举全链终验）；门 C
  回归门（全套件 + 双审计 + T1 收口 + CLI 冒烟）。落位
  tests/v0/stage2/plan/bootstrap_compiler_tests.rs（镜像
  bootstrap_expander_tests 结构——正负成对）。
- **切换点 P1-P5**：CompilerKind::{Seed,Bootstrap} 镜像
  ExpanderKind（bootstrap init 恒种子——无递归）；守护
  production_compiler_is_bootstrap；无 parity 不切换 + 单点回退；
  **缓存键分桶**（B11——种子与自举产物不得混享缓存条目）；eval
  退役终态口径（T1 断言迁移非删除 + TD-017 终验）。
- **确定性纪律 4 条**（fixpoint 先决）：入口复位（SCOPE-NEXT:1471
  延伸）+ 插入序即索引（禁哈希序依赖）+ 符号 str 携带桥回 intern +
  门 B 隔离运行（fresh 桥实例/新线程最保守形态）。
- **42-z（r20 收尾交付）**：§3.2 六命令 clean 起步全绿（build
  12.94s / check 0/0 / fmt 0 / clippy -D 0 / **638:0:0 零回归**
  ——设计轮零测试增量）；CLI 冒烟四路径 + E0002 负例（R9
  fail-closed 实证）；对账六面（RELEASE_NOTES/matrix/pipeline/
  登记册/roadmap/plan）；r20 tar.gz 包内自举验证；web 同步
  （kerf-data r20 + footer）+ git 入账 + rec 树压实（本条目）。

## 检索关键词

i1 切口 · compile_module 单点 · 三件套第三实例 · bootstrap_compiler
桥 · bytecode_equal 判据 · parity 三门 A/B/C · 自举一致性门 B ·
CompilerKind 切换 · 缓存键分桶 · SCOPE-NEXT 入口复位 · inserting
序确定性 · analyzing 不在自举关键路径 · eval 退役 INC7 · ~80% 口径
精确化 · 段序 S1-S3 DAG · 42-b 八臂 · 闭包捕获 mirror · match_bindings
子集匹配镜像
