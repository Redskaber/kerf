# r29 批次 J 执行：J2 HM 旗标期切换（48-c——会话 Task 50-a/50-z）

> Task ID: 50-a/50-z（plan §5b MUV 48-c / 批次 J 收尾序）· 2026-09-11 · 溯源：flat worklog（kerf/docs/worklog.md 尾部条目）· RELEASE_NOTES v0.4.0-r29

## 概要（1:8 压缩——详录见 flat）

- **会话恢复纪律（PHASE 4——同型第三次处置）**：续接摘要基线严重过期
  （声称 r21/657 + 42-c 为下一 MUV——42-c 实为 r22 已交付）→ 磁盘实况
  复核（git 1d2323a = r28 49-z 终态 + clean + worklog 49-z「下一步」=
  48-c J2）→ 按 plan §5b 48-c 验收合同执行。
- **判定面切换（D8 阶段 2 落地——hm-inference-design §3.7 演进轨道）**：
  driver.rs `check_source` / `check_source_recover` 两入口
  `kerf_compiler::check_program` → `kerf_compiler::hm::hm_check_program().diags`
  （接口同构零适配；**run 路径零接触——爆炸半径有界维持**）；R1-R8 **退为
  回归基线断言**（hm_inference_tests / effect_tests 双面检出纪律保留）；
  四处头注旗标期角色注记（driver.rs / hm.rs / typecheck.rs / builtins.rs
  ——R4 文档随代码）。
- **契约重定义显式登记（P0-2 兑现——design v0.1.0 → v0.2.0，Proposed →
  Active）**：保守性断言更新为「**零类型不一致误报**」口径（类型不一致
  即报——含运行期可存活的类型不一致）；**occurs/自应用误报面文档化为
  政策接受行为**（无限类型静态报错——运行期行为不承诺双向锚）；双向锚
  `static_error_is_runtime_error` **范围重锚**（超集门内维持 / occurs 面
  豁免——typecheck_tests 头注 + 函数 doc 同步）。
- **断言重锚 5 处**（语义/检出/定位不变，渲染措辞随判定面）：
  `(int . int)` / `(int . nil)` / `(α0 → α0)`（HM 结构化类型渲染——
  R1-R8 域名精确化）+ 内置元数 5 case 区间格式（「过程参数数量不匹配：
  期望 min..max 实际 n」）。
- **切换实测双缺口当场修复（超集门/零误报门经生产入口实测纪律的兑现
  ——PoC 离线语料未覆盖生产入口负例矩阵全貌）**：
  - ① hm.rs **Ordering 臂未同步 TD-011 r24 字符串全序**（`(< "a" "b")`
    曾误报「字符串仅支持 =」——PoC r18 遗留旧口径）→ all_str → 合法；
  - ② hm.rs **car/cdr 臂 `Ty::Nil` 误入保守跳过**（`(car nil)` 曾漏检
    ——原注释「R5 口径之外的动态边界」为错误归因：Nil 静态确定，运行期
    必然 E5）→ Nil 归入诊断臂（与 R1-R8 R5 负例矩阵对齐）。
- **新增旗标期判定面测试组 3 件（typecheck_tests 25 → 28）**：增值面
  生产证据（`(f "s")` 约束传播检出「类型不一致」——R1-R8 静默面，切换
  直接证据）+ occurs 豁免政策锚（报出但不断言双向锚）+ 双面修复锚
  （TD-011 零误报 ×2 + car nil 检出 + 双向锚维持）。
- **12 §2.5.1 类型检查器行「迁移评估」结论 ✅（v6.4）**：HM 旗标期切换
  落地 + **自举内迁裁定 = 留 Rust**（INC8 三段口径——「kerf ~80%」=
  读+展开+编译 100% kerf；类型检查器属静态分析面不在自举关键路径
  （INC3 实证 front_from_core 无 check），归 07 §3.4 Rust 保留面；
  默认期（R1-R8 语义内化）= Stage 2 末评估）。
- **GATE 1**：§3.2 六命令全量实测（build 零告警 / check 0/0 / fmt 0
  diff / clippy --all-targets --workspace 超集 0 / **test --workspace
  713:0:0**（单元 207 + 集成 506——710 零回归 + 净 3）/ 三审计集
  EXIT 0 ×3 + CLI 四路径含 **check 负例生产实证**（E0005「类型不一致：
  num 与 str」定位 1:24——R1-R8 时代此程序静默，HM 增值面 CLI 可见））。
- **环境归因（R1 纪律）**：沙箱重置后测试线程默认栈不足（深嵌套 case
  爆栈——**基线 stash 复跑同型归因环境而非代码**）→ RUST_MIN_STACK=
  16777216 环境级修复全绿（同 r21 工具链重装同型环境恢复操作）。
- **对账六面**：matrix r29 行（710 → 713 + 表体两行实测修正 24→28）/
  pipeline-test-coverage v0.4.0-r29（Tier 2 头 503 → 506）/ plan §5b
  J 执行注记 r29 + Status / RELEASE_NOTES v0.4.0-r29 / 12-roadmap
  v6.4 / hm-inference-design v0.2.0；TD 登记册零事件。
- **50-z 收尾**：r29 tar.gz（§19.3 commit-then-package 正序）+ 包内
  自举验证 + web 同步（kerf-data r29 三节点 + footer v6.9 +
  download README r29 节）+ root worklog 索引 + rec 树 13_r29 +
  l 两层新行 + git 双仓库。

## 下一步

48-d J3 FFI VM 面做实（write_stdout char* 按 ffi-ownership-model §8
窗口规程步 2-4 + Alloc/FreeExternal 面开放 + E0010-E0012 诊断族 +
§6 边界 13 case + 回写四处）→ 48-e J4 批次 J 收尾 → 批次 K 终批
（49-x 重分配 ID——§5c 概排注记）。
