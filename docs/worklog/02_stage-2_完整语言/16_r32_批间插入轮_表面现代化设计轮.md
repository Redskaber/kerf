# 16_r32 批间插入轮_表面现代化设计轮（Task 53-a/53-z）

> 压缩条目（1:8 口径——详录 = flat worklog 53-a/53-z 条目 + root worklog 数字详录）

## 一句话

用户审查指令「实现端不要停留在 Lisp 家族 1970 年代表达」驱动的设计先导轮：**库表面现代化规范首落**——lang-design/20-surface-conventions.md v1.0（R1-R6 命名规则 + B1-B5 行为契约 + `/` 命名空间层 + 57 项内置映射表 + 零破坏三批次迁移 v0.5/v0.6/Stage 3）+ 原则 33「表面现代化动态演进」三方同步 + TD-027 + 758:0:0 零回归维持。

## 关键裁定

| # | 裁定 | 依据 |
|---|---|---|
| 1 | 库表面 = 现代化「未认领半区」（57 内置名 `?`×18/`->`×3/car-cdr/扁平前缀——无 owner/映射/TD） | 全项目扫描实测（53-a） |
| 2 | 标点后缀约定 = 前类型系统时代补偿机制；HM/效应/能力就位后表面同步迁移 | 原则 33（17 v6.3/sop v12.1/stage0 v6.2） |
| 3 | R1-R6：全词 / `is-` 谓词前缀（Swift/Kotlin/Rust 共识）/ 动词关系裸形（contains/starts-with）/ `to`-`from` 方向词 / 库面零 `!` 新铸 / 运算符族冻结 | 20 §2-§3（知识搜索前沿对照） |
| 4 | B1-B5：index-of 哨兵→nil、member 真值多态单一化、read-line 严格元数（v0.6 契约层）；恒等元/eq 语义显式保留 | 20 §4（别名层零语义变更不变量） |
| 5 | 命名空间层：`kerf/<模块>` 七件 + 能力-命名空间对齐（`kerf/io` ↔ `(require io …)`） | 20 §5 |
| 6 | E5 适用域划分：内部 ADT + 关键字 = E5 一次性窗口（Stage 3）；库表面 = 别名渐进（纯增量零自举链接触）——不冲突证明 | 20 §6.2（r18 委员会裁定载荷分析） |
| 7 | 零破坏三段：v0.5 批次 L 别名层（27 名双注册 + parity 锚定）→ v0.6 批次 M 命名空间层 → Stage 3 移除（与 E5 关键字切换同窗） | 20 §7 + 12 §2.10（v0.5-roadmap 预锚） |
| 8 | 顺带实测对账：builtins.rs 头注 3→4 Reader 原语 + hm 文档 49→56 项 | §2.3-12 文档随实测 |

## 数字

- 测试：758:0:0 维持（零断言修改；单元 215 + 集成 543）+ 三审计集 EXIT 0 ×3（41+50+53）+ CLI 四路径
- 代码变更：1 文件（builtins.rs 注释对账——零语义）
- 文档：新文件 1（20-表面规范 ~230 行）+ 更新 11（17/09/12/18/00/02/stage0/sop/TD 登记册/matrix/pipeline/RELEASE_NOTES/plan）
- 扫描面：19 lang-design 文件 + develop + backend/vm/graph/tests/tools + 4 .krf 引导语料 + 21 测试文件 + examples

## 下一步

批次 K 终批（plan §5c：K1 终门审 stage2_gate_audit_r2 ≥30 新 case + §21.3 四条件终验 + §21.5 九信号 → K2 大阶段末深审环 → K3 收尾（v0.5-roadmap 直引 12 §2.10 表面现代化批次 L 预锚））。
