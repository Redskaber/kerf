# r21 批次 I 执行：I1 前段基础核心形式 kerf 化（三件套第三实例 + 门 A parity）

> Task ID: 42-b · 2026-09-11 · 溯源：flat worklog（kerf/docs/worklog.md 尾部条目）· RELEASE_NOTES v0.4.0-r21

## 概要（1:8 压缩——详录见 flat）

- **42-b（I1 前段基础核心形式 kerf 化——S1 段）**：
  `bootstrap/compiler.krf`（~790 行，compile.rs 798 行逐语义镜像）+
  `bootstrap_compiler.rs` 桥（~640 行）+ `bootstrap_compiler_tests.rs`
  （19 测试）——三件套第三实例（r6 Reader / r14-r15 Expander 之后）。
  **八臂全量**：Literal（标记化分派 + Bool/Nil 直发不进池 + 引号点对
  递归）/ VarRef（三源解析）/ If（跳转回填序镜像）/ Begin（末项继承
  尾位 + Pop 挂项自身 Span）/ Set!（值→DUP→存储三路径）/ Define
  （入口原型守卫 + D1 消息逐字）/ App（被调先 + 尾位 TailCall）/
  Lambda（**捕获转换全序**：自由变量出现 → 捕获解析 → 新原型 →
  新帧 → 窗口切换 → 体编译 tail=true → RET → 回写 → 弹帧 →
  CLOSURE 发射）。
- **作用域解析机逐语义镜像**（B3 最难段）：match-bindings（子集
  匹配 + max-cardinality 严格大于）+ resolve-var（逆序帧栈——首
  个含子集匹配帧胜出）+ **shadow-pop 口径逐位镜像**（形参「新名才
  入栈 + 弹出恒为形参计数」——Rust 既有行为原样保留，§12 最优>最小：
  镜像而非「修正」）。
- **确定性纪律**（§7/B8）：入口全量复位（CC-* 14 项）+ 关联列表
  首插序（禁哈希序）+ 符号 str 携带桥回 intern + const-eq? 结构
  相等（**eq? 对 Pair 引用相等——逐字段递归**；Float f64== 镜像
  HashMap Hash+Eq：±0.0 合一、NaN 永不去重）。
- **INC4 实现期修订**（实测发现——切口设计 v1.1 注记）：①字面量
  消歧标记（('int v)('float v)('str s)('sym 名)('pair l r)——VM
  值面无 Str/Symbol/Float 区分谓词，桥侧定型）；②span 三元组
  (s e exp)——bytecode_equal 含 expansion_id；③名形三态
  'main/'anon/'name + 码表 0..=40（opcode.rs 声明序）。
- **实测驱动修复三轮**（§2.3-11）：括号失衡两处（逐行平衡脚本）；
  **语言陷阱**——'true/'false/'nil 是 bool/nil 字面量非符号（标签
  分派改 symbol->string 字符串比较，expander.krf tname 同款）；
  错误消息全角括号（parity 红测暴露）。
- **门 A parity（19 测试，≥8 超额）**：bytecode_equal 全结构判据
  13（字面量/常量池去重/点对/回填/begin 尾位/嵌套捕获三链/遮蔽/
  set! 三路径/尾位穿线/深嵌套 100 层/双跑/空程序）+ 负例 2（D1
  消息+Span 逐字；**module/require 42-c 边界不对称断言**）+ 行为面
  4（fib 144 / closures (4 2) / map 平方 / **10 万层深尾递归**——
  编译段唯一被测变量的隔离设计）。
- **R4 发现项**：opcode_count 漏列 TailCall（r18 引入——enum 41 vs
  断言 40 三方漂移）→ opcode.rs + 04-bytecode-vm 三处同步（40→41）；
  发现路径 = 门 A 全量对账。
- **收尾（42-z'）**：657:0:0（638 零回归 + 19）；§3.2 六命令 clean
  起步全绿；CLI 冒烟 + 双审计 EXIT 0；文档五面（plan/切口设计 v1.1/
  matrix/pipeline/RELEASE_NOTES）；r21 tar.gz（302 条目）包内自举
  验证 657 + CLI 一致；web 同步 + git + 本 rec。

## 状态与索引

- 测试口径：**657:0:0**（单元 202 + 集成 455——净 +19 集成）
- 42-b 边界：parity 影子路径（生产未切换——CompilerKind 属 42-d）
- 下一步：42-c module/require 两臂 + 糖全管线 parity（门 A 扩展组
  ≥12）；42-d 生产切换 + fixpoint（门 B）+ eval 退役
- 检索锚：`compiler.krf` / `bootstrap_compiler.rs` /
  `bootstrap_compiler_tests` / `INC4 修订` / `shadow-pop` / `门 A`
